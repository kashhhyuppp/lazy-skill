/**
 * Contact address printed on the privacy and terms pages.
 *
 * A privacy policy needs a real address someone can actually write to, and
 * this one is a placeholder — set it before launch. It is deliberately not a
 * personal inbox: whatever goes here becomes public on a page crawlers read.
 */
export const CONTACT_EMAIL = "hello@lazyskill.invalid";

/** Bumped whenever either document changes materially. */
export const LEGAL_UPDATED = "3 September 2026";
